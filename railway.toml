[build]
builder = "NIXPACKS"

[deploy]
startCommand = "playwright install chromium --with-deps && python -m uvicorn api.server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
